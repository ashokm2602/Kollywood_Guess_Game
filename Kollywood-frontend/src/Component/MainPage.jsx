import { useDispatch, useSelector } from "react-redux";
import { useEffect } from "react";
import { FetchMovies } from "../Slices/MovieSlice";

export default function MainPage({ movieId }) {
  
  
}
